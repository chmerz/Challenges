<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>All the Memes</title>
    </head>
    <body>
        <nav>
            <ul>
                <li><a href="/">Home</a></li>
            </ul>
        </nav>
        <% for(let i = 0; i < image.length; i++) { %>
            <a href = "/meme/<%= i %> "><img src= "<%= image[i] %>" /></a>
        <% } %>
    </body>
</html>
